fn hash(content: string): string
{
  if content.size() > 0 {
    var i: u64 = 0;
    for i < content.size() {
      content[i] = i % 255;
      i += 1;
    }
    return content
  } else {
  return ""
  }
} 
